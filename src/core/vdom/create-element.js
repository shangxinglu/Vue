
import {
    isUndef,
    isDef,
    isTrue,
    isObject,
    isPrimitive,
} from '../util/index';

/**
 * @description 创建节点
 * 
 * @param {Object} context 组件对象
 * @param {String} [tag] 标签
 * @param {Object} [data] 节点数据
 * @param {Object|Array} [children] 子节点
 * @param {Number} [normalizationType] 格式化类型
 */
export function _createElement(context,tag,data,children,normalizationType){
    


}
